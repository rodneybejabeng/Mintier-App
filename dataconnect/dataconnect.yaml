specVersion: "v1"
serviceId: "mintier-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mintier-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
